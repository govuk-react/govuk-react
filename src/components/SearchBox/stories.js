import React from "react";
import { storiesOf } from "@storybook/react";

import SearchBox from ".";

storiesOf("SearchBox", module).add("SearchBox", () => (
  <SearchBox>SearchBox example</SearchBox>
));

import styled from 'styled-components';

const GovukAccordionSectionHeading = styled('h2')({
  marginTop: 0, // Override browser default
  marginBottom: 0, // Override browser default
});

export default GovukAccordionSectionHeading;

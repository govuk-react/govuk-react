import * as React from 'react';

import { Input } from '.';

export default {
  title: 'Form/Input',
  id: 'input',
  component: Input,
};

export const Default: React.FC = () => <Input type="text" />;

import * as React from 'react';

import { Label } from '.';

export default {
  title: 'Form/Label',
  id: 'label',
  component: Label,
};

export const Default: React.FC = () => <Label>Example</Label>;
